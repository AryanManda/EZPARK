To run the website locally on your machine, unzip node_modules into this directory and open the command prompt or powershell, then cd to the file location and run
"npm run dev"

Then, input "o" once the menu pops up and "q" to quit.
